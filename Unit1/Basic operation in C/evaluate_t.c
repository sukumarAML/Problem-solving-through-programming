#include <stdio.h>
int main()
{
    int a = 15;
    int b = 4;
    float y;
    int s = a / b;
    x = a % b;
    y = 15.0/4.0;
    int p = 6*2-4;
    int q =(6*2)-(10%3);

    printf("The sum of %d and %d is %d, %d, %.2f, %d, %d\n", a, b, s, x , y, p, q);

    return 0;
}   