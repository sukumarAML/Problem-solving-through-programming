#include<stdio.h>
int main()
{
  printf("Addition of two numbers:\n");
  int a,b,sum;
  scanf("Enter the first number:%d", a);
  scanf("Enter the second number:%d", b);
  sum = a+b;
  printf("the sum of %d and %d is %d",a,b,sum);
}
