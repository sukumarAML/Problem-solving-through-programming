#include<stdio.h>
int main()
{
    int value;
    scanf("%d", &value);
    int max = (if((Value % 2 == 0) || (Value % 3 == 0) || (Value % 5 == 0) || (Value % 7 == 0)))?(value%2==0):value;
    printf("%d",max);
    return 0;
}