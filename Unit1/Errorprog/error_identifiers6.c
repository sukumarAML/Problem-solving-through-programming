#include <stdio.h>

int main()
{
    int total#marks = 450;

    printf("%d", total#marks);

    return 0;
}
// Identifier containing a '#' character is invalid in C. Identifiers can only contain letters, digits, and underscores, and cannot start with a digit.