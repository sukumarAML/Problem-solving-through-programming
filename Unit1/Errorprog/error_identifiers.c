#include <stdio.h>

int main()
{
    int 2marks = 85;

    printf("%d", 2marks);

    return 0;
}
//The code you provided has a syntax error because variable names in C cannot start with a digit. In this case, `2marks` is an invalid identifier. You should rename the variable to start with a letter or an underscore. Here’s a corrected version of your code: